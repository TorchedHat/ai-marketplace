{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Inductor Expert Response",
  "description": "Structured response schema for inductor-expert-agent",
  "type": "object",
  "required": ["specialist", "version", "task", "confidence", "insight", "files", "guidance"],
  "properties": {
    "specialist": {
      "const": "inductor-expert-agent",
      "description": "Agent identifier"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Agent version (semver)"
    },
    "task": {
      "type": "string",
      "description": "Original question or task"
    },
    "confidence": {
      "type": "string",
      "enum": ["high", "medium", "low"],
      "description": "Confidence level in the response"
    },
    "insight": {
      "type": "string",
      "description": "One-sentence key finding"
    },
    "files": {
      "type": "array",
      "items": {"type": "string", "pattern": "^[^:]+:\\d+"},
      "description": "List of file:line references"
    },
    "concepts": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Key concepts involved (e.g., Triton, fusion, lowering)"
    },
    "guidance": {
      "type": "string",
      "description": "2-3 paragraph explanation of why/how"
    },
    "code": {
      "type": "string",
      "description": "Minimal runnable code example or kernel snippet"
    },
    "steps": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Actionable steps with file:line references"
    },
    "dependencies": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Prerequisites needed"
    },
    "pitfalls": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Common mistakes to avoid"
    },
    "skill_references": {
      "type": "array",
      "items": {"type": "string"},
      "description": "References to skill sections"
    },
    "handoff": {
      "type": ["object", "null"],
      "properties": {
        "to_agent": {"type": "string"},
        "reason": {"type": "string"},
        "context": {"type": "object"}
      },
      "description": "Optional handoff to another agent"
    }
  }
}
