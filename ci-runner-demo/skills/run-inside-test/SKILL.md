---
name: run-inside-test
description: Test skill that runs a script from inside Claude. Use to verify that Claude can execute plugin-bundled scripts via ${CLAUDE_PLUGIN_ROOT}.
---

# Run Inside Test

Run the bundled test script and report its output.

## Instructions

1. Run this script with bash: `${CLAUDE_PLUGIN_ROOT}/scripts/run_inside.sh`
2. Report the exact output.
