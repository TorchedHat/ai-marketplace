name: inductor-debugger-agent
version: 1.0.0
description: Debug PyTorch Inductor lowerings, fusion, and Triton codegen

# Reference agent definition
agent_definition: ../../agent-plugins/inductor-debugger-agent/agents/inductor-expert.md

# Skills path (bundled from vertical-plugins/)
skills_path: ../../agent-plugins/inductor-debugger-agent/skills/

# MCP connectors
mcp_config: ../../agent-plugins/inductor-debugger-agent/.mcp.json

# Allowed tool calls (must match agent frontmatter)
tools:
  allowed:
    - Read
    - mcp__steering__query_api_docs
    - mcp__steering__query_steering
  denied:
    - Write
    - Bash
    - Edit

# Agent-to-agent calls
callable_agents:
  - coordinator-agent
  - dynamo-expert-agent  # For handoffs when issue spans stages

# Deployment settings
deployment:
  max_turns: 5
  timeout_seconds: 300
  retry_on_error: true
  mode: interactive
